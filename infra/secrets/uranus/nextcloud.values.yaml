nextcloud:
  host: uranus-cloud.duckdns.org
  username: saitama
  password: "J0s3$miliano88"
  email: "devadalberto@gmail.com"

ingress:
  enabled: true
  className: traefik
  hosts:
    - host: uranus-cloud.duckdns.org
      paths:
        - path: /
          pathType: Prefix
  # leave tls empty for now; Traefik is already terminating HTTP
  tls: []

internalDatabase:
  enabled: false

mariadb:
  enabled: false

postgresql:
  enabled: false

externalDatabase:
  enabled: true
  type: postgresql
  host: postgresql.uranus-db.svc.cluster.local
  port: 5432
  database: uranus_homelab
  user: uranus_app
  password: ""
  existingSecret:
    enabled: true
    secretName: uranus-nextcloud-db
    usernameKey: username
    passwordKey: password

